# ── Discord ────────────────────────────────────────────────
DISCORD_TOKEN=
DISCORD_APP_ID=
DISCORD_GUILD_ID=

# ── GitHub App (NOT a Personal Access Token) ───────────────
GITHUB_APP_ID=
# Paste the PEM contents on one line with literal \n, or use a multiline value.
GITHUB_APP_PRIVATE_KEY=
GITHUB_WEBHOOK_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# ── Webhook server ─────────────────────────────────────────
PORT=3000
WEBHOOK_PATH=/github/webhook

# ── Database (SQLite by default, PostgreSQL optional) ──────
DATABASE_URL="file:./prisma/dev.db"

# ── Runtime ────────────────────────────────────────────────
NODE_ENV=development
LOG_LEVEL=info
CONFIG_PATH=./config.yaml

# ── Deployment (docker-compose.deploy.yml) ─────────────────
# The image built by CI. In Docker the DATABASE_URL is overridden to
# file:/data/prod.db (a persisted volume) — no need to set it here.
BOT_IMAGE=ghcr.io/OWNER/discord-forum-issues-bot:latest
